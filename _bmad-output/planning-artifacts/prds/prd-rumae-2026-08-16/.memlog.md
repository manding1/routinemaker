---
topic: MANDING
updated: 2026-08-17T10:35
---

- (decision) Working mode: Coaching path, entry point Journey-led (chosen by user)
- (decision) Stakes: hobby/solo (single-user personal tool, no login/server)
- (assumption) Brain dump inputs confirmed sufficient: handoff-prompt_1.md + DESIGN-apple.md, no additional sources
- (decision) Grape weighting resolved: each completed action = 1 grape regardless of size, but actions are scoped per user-defined topic/unit (e.g. '달리기 1.5km', '독서 1챕터') rather than raw arbitrary micro-steps — user defines the unit, oversized units still get split via existing 더 작게 flow
- (decision) Celebration: keep both — first grape of the day triggers 어제보다 나은 오늘이에요, AND separately when today's total surpasses yesterday's total, a second celebration (어제를 넘었어요) fires
- (decision) New feature: memo/note capability on grapes — user wants to write that day's review
- (decision) Memo feature scoped to daily review only (written at end-of-day when 이미 완료 is pressed), not per-grape memos
- (decision) UJ-2 added: post-graduation routine-mode day, represented as a simple checklist UI
- (assumption) OPEN: routine graduation trigger criteria not yet finalized — original doc only gave '3+ different days' as an example, not a locked rule
- (decision) Routine-mode checklist items do NOT earn grapes — grapes are exploration-mode only
- (decision) Architecture clarified: routine and exploration are NOT mutually exclusive app-wide modes — they are per-item states that coexist same day. A user has (a) graduated routine items shown as a checklist (no grapes) running alongside (b) the ongoing 다음 한 걸음 exploration loop (earns grapes) for items still being discovered/repeated
- (decision) Routine graduation trigger: when a specific step/topic's own grape cluster (포도송이 = 15 grapes) fills completely, that item moves to routine mode — replaces the original vague '3+ different days' rule
- (decision) Global lifetime grape-cluster counter (포도송이 ×N, uncapped) removed entirely. Grape clusters now exist only per-topic, purely as a graduation mechanism (15/15 = graduate to routine)
- (decision) Per-topic cluster progress IS shown on screen (not hidden/surprise). Completion interaction: tapping the grape-cluster sticker board itself marks the action done and fills the next grape slot — replaces a generic [완료] button. Visual ref: triangular 15-slot grape board (5-4-3-2-1 rows) with a header label and a small badge callout ('작은 성취 맛보기' style), provided by user as an image mockup
- (decision) Confirmed: multiple topic tracks run in parallel (not one global current-step card). Each topic ('달리기','독서', etc.) is its own track with its own current-step loop and its own grape cluster. New tracks are created via a [새로운 포도송이 추가] button, seeded with just a topic name — no upfront breakdown of steps within that topic
- (decision) Cluster board label simplified: no per-cluster ordinal ('N개째') — replaced with a running count of currently-active (non-graduated) topic tracks, phrased 'N개의 포도송이 키우는 중'
- (decision) SUPERSEDES entry 5: celebration simplified — '어제보다 나은 오늘입니다!' fires on EVERY exploration-mode grape tap (not just the first of the day), and the separate 'surpassed yesterday's total' celebration is dropped. Routine-checklist taps never trigger this celebration (checklist is unrelated to the celebration mechanic)
- (assumption) DEFERRED (non-blocker): whether a topic can start a fresh new cluster after already graduating to routine mode is unresolved. Working assumption: 키우는 중 count only reflects active non-graduated topics, graduated topics simply stop accumulating grapes. Revisit if user wants re-attempt/relapse handling
- (decision) Daily review is structured into 4 sub-sections: 잘한점 (what went well), 개선할 점 (what to improve), 감사일기 (gratitude journal), 확언 (affirmation)
- (event) First full PRD draft written (17 FRs, 2 UJs, MVP scope, 3 open questions, 3 tagged assumptions)
- (decision) RESOLVES Open Question 1: removed the '다음 하나는?' conversational question entirely. After completing a step, only the celebration message shows (어제보다 나은 오늘입니다!) — no question prompt. The current-step field simply sits empty afterward, waiting for the user to type a new step whenever they're ready, with no forced prompt. Per-track [이미 완료] button (old FR-8) is removed as a consequence — there is nothing left to dismiss since no recurring question exists. A track that the user doesn't touch just stays quietly empty; no explicit stop action needed
- (event) PRD revised: removed 다음 하나는? question flow and per-track 이미 완료 button, FRs renumbered (16 FRs total), Open Question 1 resolved
- (decision) RESOLVES Open Question 2: graduated routine items get two management actions — [다시 도전하기] (restart as a fresh 0/15 exploration track, same name) or [삭제하기] (remove from checklist permanently). Both offered rather than picking just one
- (event) PRD updated: FR-14 added (졸업 항목 다시 도전하기/삭제하기), FRs renumbered to 17 total, Open Questions reduced to 1 (어제 총알 앰비언트 표시), fixed FR-6 cross-reference typo (FR-11→FR-10)
- (decision) New rule: each 포도송이 (topic) can earn at most 1 grape per calendar day. Once today's grape is earned, the current-step input stays editable (e.g. jotting tomorrow's step ahead of time) but tapping does not add another grape that day — resets next day. This also naturally aligns graduation (15/15) with '15 distinct days of engagement', echoing the original doc's multi-day repetition intent
- (decision) Terminology simplified: drop the '탐색 트랙' glossary term entirely — '포도송이' is now the single term for a topic/entity (matches the user's own vocabulary: 포도송이 추가, 포도송이 키우는 중). '트랙' no longer appears anywhere in the PRD
- (event) PRD fully revised: '트랙' terminology removed, replaced with 포도송이 throughout. Added FR-6 (1 grape/day/포도송이 cap). FRs renumbered to 18 total. Open Questions now 2 (어제 총알 표시, FR-6 UI reaction on cap-hit)
- (decision) 기상 미세동작(dedicated wake-trigger feature, old FR-1/FR-2) removed entirely. No special hardcoded onboarding step — the user defines their own first 포도송이/걸음 like any other, since goal/step content is always user-authored anyway. All FRs renumbered (FR-1~FR-16)
- (decision) Reconciliation gaps addressed: added Cross-Cutting NFRs section (PWA installability/manifest, localStorage persistence, mobile portrait one-handed, offline single-file, date-keyed data) per original brief's explicit 3x-stated requirement that had been silently dropped. Added a Design Principles subsection restoring the original 7 절대 원칙 verbatim with status notes, including honest acknowledgment that Principle 4 (ask right after finishing) was adapted since the explicit question prompt was removed. Added [NOTE FOR PM] on the daily-review FR flagging the 확언/감사일기 vs Principle 7 tension as a conscious accepted choice
- (event) Finalize: reconciliation done (2 subagents), reviewer rubric self-pass done (review-rubric.md), 3 thin FRs (FR-2/FR-8/FR-11) given testable consequences, addendum.md updated with confirmed style-tension nuance and verb-form UX note
- (decision) RESOLVES Open Question 1 (revised): no ambient yesterday-count display. Instead, two new daily-total celebrations added: (a) fires when today's total grapes (distinct 포도송이 tapped today) exceeds yesterday's total, (b) fires when today's total reaches/exceeds the all-time daily best. These are separate from the existing per-tap celebration (FR-7). FRs renumbered again: FR-1~FR-7 unchanged, new FR-8/9/10 inserted, everything from old FR-8 onward shifts +2 (final range FR-1~FR-19)
- (decision) RESOLVES Open Question 2 (delegated to facilitator): tapping a 포도송이 that already got today's grape shows a brief, low-pressure inline message (e.g. '오늘 몫은 이미 받았어요, 내일 또 만나요') with no error/warning styling, then nothing else happens. Exact copy/animation left open for UX phase but behavior is decided
- (decision) Visual reference added: user supplied an image of a glossy magenta/purple jelly sphere ('쫄깃한 젤리 느낌') as the intended look for a 포도알. Captured to addendum.md for UX spec handoff
- (decision) Style tension resolved: DESIGN-apple.md takes priority. The grape-cluster sticker-book mockup and jelly-texture grape reference are aspirational/mood inputs only — if they conflict with the Apple single-accent minimalist system, drop or adapt them rather than forcing both styles to coexist
- (event) Finalize complete: added FR-8/9/10 (daily-total celebrations), resolved both remaining Open Questions, resolved Apple-vs-grape-mockup style tension in addendum.md (Apple wins), all triage items closed. Ready to set status: final
- (event) PRD finalized
- (change) FR-4 cap-hit copy finalized to '오늘 완료! 내일 만나요:D' during UX preview review; Assumptions Index updated
