# 인수인계 프롬프트 — "MANDING" 웹서비스 MVP

> 이 문서는 개발자 또는 코딩 AI에게 그대로 전달하기 위한 빌드 지시서입니다.
> 아래 내용을 읽고, **단일 HTML 파일 하나로 동작하는 모바일 웹앱 프로토타입**을 만들어 주세요.

---

## 0. 한 줄 요약

서비스 이름은 **MANDING** 이다. 의지력이 아니라 '인지 설계'로 하루를 굴러가게 만드는 개인용 웹앱.
**에너지를 많이 쓰는 '탐색용 발자국'을, 에너지를 안 쓰는 '자동 루틴'으로 승격시키는 기계.**

- **모토:** "어제보다 나은 오늘"
- **슬로건:** 의지력이 아닌 '인지 설계'로 완성하는 자동화된 일상
- **사용자:** 지금은 딱 한 사람(제작자 본인)만을 위한 도구. 로그인·서버·회원 개념 없음.

---

## 1. 페르소나 (설계의 모든 결정은 이 사람 기준)

- 아침형 인간이지만 **아침에 일어나는 것 자체를 힘들어한다.**
- 하고 싶은 게 많고 **목표가 높다.**
- **작은 성취부터 맛보고 싶어 한다.**
- 최종적으로 원하는 건 **대부분의 일을 루틴화해서 에너지를 거의 안 쓰는 상태.**

이 사람은 큰 산을 보여주면 "내가 겨우 이거…" 하고 좌절한다. 그래서 **높은 최종 목표는 절대 화면에 %로 들이밀지 않는다.** 화면엔 오직 "지금 밟을 돌 하나"와 "쌓여가는 발자국"만 보인다.

---

## 2. 절대 원칙 (이걸 어기면 이 앱이 아니다)

1. **최종 목표를 진도율(%)로 보여주지 않는다.** 분모가 최종 목표인 순간 좌절이 생긴다.
2. **미리 계획을 다 쪼개게 시키지 않는다.** 전체 분할은 에너지 낭비이자 미래를 과잉 예측하게 만들어 자괴감을 부른다.
3. **항상 '바로 다음 딱 하나'만 존재하게 한다.** 계획은 미리 그린 지도가 아니라, 걸으면서 뒤에 남는 발자국이다.
4. **다음 단계는 '하기 전'이 아니라 '방금 하나를 끝낸 직후'에 묻는다.** 그 순간이 심리적으로 가장 가볍다(관성이 붙은 상태).
5. **실패는 혼내지 않고 "더 작게 쪼개라는 신호"로 다룬다.**
6. **비교 대상은 최종 목표가 아니라 '어제의 나'다.** (모토: 어제보다 나은 오늘)
7. **감정("열심히 하겠다")이 아니라 행동("10분 걷기")을 기록한다.**

---

## 3. 핵심 메커니즘 — "다음 한 개만" 루프

### 3-1. 하루의 첫 발 = 기상 미세동작
이 페르소나에게 하루의 진짜 첫 트리거는 '과업'이 아니라 **몸을 침대에서 떼는 것**이다.
- 앱을 아침에 처음 열면, 첫 화면은 공부/작업이 아니라 **기상 미세동작** 하나를 제시한다.
  - 예: "두 발 바닥에 대기", "물 한 잔 마시기", "커튼 열기"
- 문구는 편집 가능(사용자가 자기만의 첫 발로 바꿀 수 있음). 큰 [완료] 버튼 하나.

### 3-2. 완료 직후 → "다음 한 걸음은?" or "이미 완료"
- 사용자가 현재 걸음을 [완료]하면, 앱은 계획을 짜라고 하지 않는다.
- 딱 이렇게 묻는다: **"방금 걸 했으니, 지금 바로 이어서 할 수 있는 다음 하나는?"** — 그리고 이 화면에 **[이미 완료]** 버튼을 함께 둔다.
- 다음 걸음을 한 줄 입력하면 그게 새로운 '현재 걸음'이 되어 반복된다.
- **[이미 완료]** 를 누르면 오늘의 걷기를 여기서 종료한다(더 이상 다음을 묻지 않음). 오늘 걸음 수가 확정되고 '어제보다 나은 오늘' 비교로 마무리된다.

### 3-3. 현재 걸음 카드 (메인 UI)
언제나 화면 중앙엔 **오직 하나의 현재 걸음 카드**만 크게 보인다. 버튼 두 개:
- **[완료]** → 포도알 +1, 다음 걸음 질문으로.
- **[너무 커요 → 더 작게]** → 혼내지 않고 "그 단계가 아직 너무 컸나 봐요. 그거보다 더 작은 한 발은?" 하고 더 작은 입력을 받아 현재 걸음을 교체.

### 3-4. 포도알 스티커 (진도 시각화)
- 퍼센트/분모 없음. 완료할 때마다 **포도알이 하나씩 송이에 톡 채워진다**(어릴 적 포도알 붙이기 스티커 느낌).
- **포도송이 1개 = 포도알 15개.** 현재 송이에 15알이 다 차면 그 송이는 '완성'되고, **완성된 송이 개수가 하나 올라간다(포도송이 ×N).** 그다음 알부터는 새 빈 송이의 1번째 알로 이어진다.
- 화면에는 **완성된 송이 개수(예: 포도송이 ×3)** 와 **지금 채우는 중인 송이의 알 개수(예: 7/15)** 를 함께 보여준다.
- **끝이 없어야 함:** 송이 개수에는 상한/목표선이 없다. "여기까지 채우면 끝"이라는 완결 지점을 만들지 않는다.

### 3-5. "어제보다 나은 오늘" (핵심 비교 장치)
- 화면에 **어제의 총 포도알 수**를 항상 은은하게 표시한다. (예: "어제 7알")
- 오늘 포도알이 어제를 넘는 순간 축하 연출: **"🎉 어제를 넘었어요!"**
- 이게 이 앱의 유일한 '목표선'이다. 이기기 쉽고, 매일 이길 수 있다.

### 3-6. 루틴 졸업 (이 앱의 종착점)
- 같은 걸음(같은 문구)이 **여러 날(예: 서로 다른 3일 이상)** 반복 완료되면, 앱이 감지하고 제안한다:
  - **"이 걸음, 요즘 자주 밟네요. 아예 루틴으로 고정할까요?"**
- 루틴으로 고정되면, 그 다음부터는 "다음은 뭐지?"라고 묻지 않고 **순서를 자동으로 착착 내밀어 준다**(결정 비용 0 → 에너지 절약).
- 즉 앱의 상태는 두 모드로 나뉜다: **탐색 모드**(한 걸음씩 더듬음) vs **루틴 모드**(익숙해진 순서를 자동 재생).

---

## 4. 화면 / 상태 흐름 (간단 상태 머신)

1. **START (아침 첫 화면)**: 인사 + 현재 시각 + 기상 미세동작 카드 + [완료]
2. **STEP (현재 걸음)**: 현재 걸음 카드 + [완료] / [너무 커요]
3. **ASK_NEXT (완료 직후)**: "다음 한 걸음은?" 한 줄 입력 → STEP으로 / 또는 **[이미 완료]** → END(오늘 종료)
4. **SHRINK (너무 커요 눌렀을 때)**: "더 작은 한 발은?" 입력 → STEP으로
5. 상단/하단 고정 영역: 오늘 완성한 포도송이 개수(포도송이 ×N)와 현재 송이 진행(◯/15), "어제 N알" 비교, 어제 넘으면 축하 배너
6. **루틴 제안 배너**: 조건 충족 시 STEP 화면 위에 슬쩍 등장
7. (선택) **END / 회고**: 하루 마무리 시 "오늘 어제보다 나았나요?" 한 줄 + 감정 아닌 행동 기록

---

## 5. 데이터 모델 (전부 기기 내 localStorage, 서버 없음)

```js
// 날짜별 걸음 기록
days = {
  "2026-08-16": {
    steps: [ { text: "물 한 잔", doneAt: "07:12" }, ... ]  // 완료된 걸음들
  },
  ...
}

// 학습된 루틴 후보 (문구별로 완료된 '서로 다른 날' 수를 셈)
stepStats = { "물 한 잔": ["2026-08-14","2026-08-15","2026-08-16"], ... }

// 확정된 루틴 (자동 재생용, 순서 보존)
routines = [ { text: "물 한 잔", order: 0 }, ... ]
```

- 오늘 걸음 수(= 오늘 포도알) = `days[today].steps.length`
- 어제 걸음 수 = `days[yesterday].steps.length` (없으면 0)
- 완성 송이 = `Math.floor(오늘포도알 / 15)`, 현재 송이 진행 = `오늘포도알 % 15` (송이당 15알)
- 루틴 졸업 제안 = `stepStats[text].length >= 3` 인데 아직 `routines`에 없을 때

---

## 6. 기술 스택 & 제약 (빠른 시행이 최우선)

- **웹으로 간다. 네이티브 앱 X.** (앱스토어 심사·양대 플랫폼 개발은 검증 단계에서 낭비)
- **단일 HTML 파일 하나.** CSS·JS 전부 인라인. 외부 라이브러리·CDN 의존성 없음 → 오프라인에서도 동작.
- **데이터는 localStorage에 기기 저장.** "어제보다 나은 오늘"은 어제 기록을 기억해야 성립하므로 로컬 영속성이 필수.
- **모바일 세로 화면 우선(폰 비율).** 큰 탭 타깃, 한 손 조작.
- **PWA로 '홈 화면에 추가' 가능하게.** manifest + 적절한 meta 태그로, 아이콘 누르면 주소창 없이 전체화면으로 뜨게. (아침에 폰 들자마자 아이콘 하나 눌러 "다음 한 개만" 화면 진입)
- 로그인/서버/DB 전부 불필요. 나중에 남들도 쓰게 될 때 붙인다.

---

## 7. 기존 6대 기능 → 이번 MVP 반영 매핑

| 원래 기능 | 이번 MVP에서 | 상태 |
|---|---|---|
| 1. 골든타임 분석 | '아침 첫 화면 = 기상 미세동작'으로 아침 골든타임을 붙잡는 형태로 축소 반영 | 부분 반영 |
| 2. 트리거/단계별 시작 | "다음 한 개만" 루프 + 기상 미세동작이 곧 이것 | **핵심 반영** |
| 3. 21분할 미세계획 | **미리 쪼개기 폐기.** 대신 '완료 직후 다음 한 걸음 유도' + '막히면 더 작게'로 대체 | **재설계 반영** |
| 4. 인지효율 과업배치(오전 딥싱크/오후 루틴) | MVP엔 자동 배치까진 X. '루틴 졸업'이 이 철학의 씨앗 | 후순위 |
| 5. 동사형 목표 | 걸음을 행동(동사) 문구로 적게 유도(입력 힌트로). 명사형 목표선 없음 | 톤으로 반영 |
| 6. 행동 약속 / 루틴 변형 | '어제보다 나은 오늘' 비교 + '루틴 졸업'. 지루함 방지 변형은 후순위 | 부분 반영 |

---

## 8. 이번에 만들 MVP 범위

**만들 것 (필수):**
- 기상 미세동작 첫 화면
- "다음 한 개만" 루프(완료 → 다음 질문), [너무 커요 → 더 작게]
- 포도알 누적(15알 = 포도송이 1개 완성, 완성 송이 개수가 ×N으로 올라감)
- "어제 N걸음" 비교 + 어제 넘으면 축하
- 루틴 졸업 감지 & 제안(+ 확정 시 자동 순서 재생)
- localStorage 영속화, PWA 홈화면추가

**지금은 안 함 (후순위):**
- 오전/오후 자동 과업 배치(기능 4)
- 루틴 변형/지루함 방지 제안(기능 6 후반)
- 서버·계정·동기화·통계 대시보드

---

## 9. 빌더에게 주는 최종 지시

> 위 명세대로 **단일 HTML 파일 하나**로 동작하는 모바일 웹앱을 만들어라.
> **최대한 단순하게 만들어라.** 이 문서는 기능만 규정한다. 톤·색·레이아웃 같은 디자인은 규정하지 않았으니 네가 알아서 최소한으로 처리해라(화려함보다 단순·명료 우선).
> 외부 의존성 없이 파일만 열면 즉시 돌아가야 하고, 폰에서 '홈 화면에 추가'하면 앱처럼 전체화면으로 떠야 한다.
> 절대 원칙(2번 섹션)을 UI/문구/로직 어디에서도 어기지 마라. 특히 **최종 목표를 %로 보여주지 말고, 미리 다 쪼개게 하지 말고, 비교는 언제나 '어제의 나'와 하라.**
> 완성 후, 사용자가 며칠 실제로 써보며 흐름을 검증할 수 있도록 데이터가 날짜별로 잘 쌓이는지 반드시 확인하라.
